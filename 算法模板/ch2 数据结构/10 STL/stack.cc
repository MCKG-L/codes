#include <iostream>
#include <stack>
// #include <deque>
using namespace std;
int main()
{
    stack<int> s;
    // s.size(); 大小
    // s.push(); 向栈顶添加元素
    // s.empty(); 是否为空
    // s.top(); 返回栈顶元素
    // s.pop(); 弹出栈顶元素
}