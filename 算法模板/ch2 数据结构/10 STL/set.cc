#include <iostream>
#include <set>
using namespace std;
int main()
{
    set<int> s;
    /*
    size()
    empty();
    clear();
    */
    //set multiset
    /*
    insert();
    find();返回迭代器
    count();
    erase()
        参数：
        (1) 是一个数：删除所有x O(k+lonN);
        (2) 迭代器：删除这个迭代器

    //核心操作
    lower_bound() 返回大于等于x的数的迭代器
    upper_bound() 返回大于x的数的迭代器
    */
}