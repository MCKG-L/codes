#include <iostream>
#include <cstring>
#include <algorithm>
#define IOS ios::sync_with_stdio(false);cin.tie(0);cout.tie(0)
#define endl '\n'
#define int long long
using namespace std;
/*存储方式：和堆的存储方式相同->使用一维数组
父节点：[x/2] -> x >> 1
左子节点：2x  -> x << 1
右子节点：2x+1-> x << 1 | 1
所需空间大小：4*n
*/
//基本操作
void build(int u,int L,int R){
    
}
//pushup操作
void pushup(int u){

}
//修改操作 单点修改直接写 区间修改 用到pushdown操作
void modify(int l,int r){

}
//
void solve(){
    
}
signed main()
{
    IOS;int t = 1;
    // cin >> t;
    while(t--) solve();
    return 0;
}
